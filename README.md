# terraform_vsphere

Create terraform.tfvars and put admin's credentials there

"vsphere_user" = "admin"
"vsphere_password" = "passw"

Then continue with:
terraform init
terraform plan
terraform apply